<%= render :partial => 'nav_buttons' %>

<% title "#{t(:best_recetas_of)} '#{@category.name}'" %>
<h2 class="title"><%= yield(:title) %></h2>

<%= render :partial => 'list_recetas', :locals => {:recetas => @recetas} %>

<div style="margin-top:28px;">
	<%= link_to t(:Back), recetas_path %>
</div>