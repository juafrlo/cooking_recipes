
<table>		
  <tr>
    <th>Name</th>
    <th>Puntuation</th>
    <th>Description</th>
    <th>Category</th>
    <th>User</th>
    <th style="text-align:center;">Options</th>

  </tr>

<% for receta in @recetas %>
  <tr>
    <td><%=h receta.name %></td>
    <td><%=h receta.puntuation %></td>
    <td><%=h receta.description %></td>
    <td><%=h receta.category_id %></td>
    <td><%=h receta.user_id %></td>
    <td><%= link_to 'Show', receta %> | <%= link_to 'Edit', edit_receta_path(receta) %> |
    <%= link_to 'Destroy', receta, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nueva receta', new_receta_path %>
