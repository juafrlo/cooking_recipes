<% form_for(@advice) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :title, t(:Title) %><br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :description, t(:Description) %><br />
    <%= f.text_area :description %>
  </p>

  <p>
    <%= form_template_button(f) %>
  </p>
<% end %>

<%= link_to 'Back', advices_path %>