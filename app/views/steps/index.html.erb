<h1>Listing steps</h1>

<table>
  <tr>
    <th>Receta</th>
    <th>Explanation</th>
  </tr>

<% for step in @steps %>
  <tr>
    <td><%=h step.receta_id %></td>
    <td><%=h step.explanation %></td>
    <td><%= link_to 'Show', step %></td>
    <td><%= link_to 'Edit', edit_step_path(step) %></td>
    <td><%= link_to 'Destroy', step, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New step', new_step_path %>
