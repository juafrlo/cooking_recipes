<ul class="nav_buttons">
  <li>
		<%= friends_link('Mis amigos', 'mis_amigos',
		  current_user.friends.size)%>
  </li>
  <li>
		<%= friends_link('Invitaciones recibidas', 'invitaciones_recibidas',
		  current_user.received_invitations.size)%>
	</li>
	<li>
		<%= friends_link('Mandar invitaciones', 'invitar')%>
	</li>
	<li>
		<%= friends_link('Invitaciones enviadas', 'invitados',
		  current_user.invited_friends.size)%>
	</li>
</ul>

<%= render :partial => 'friends_list',
  :locals => {:users => @users, :title => @title}%>

