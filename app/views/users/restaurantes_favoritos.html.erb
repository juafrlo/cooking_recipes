<ul class="nav_buttons">
  <li>
	  <%= favorites_link(t(:My_published_restaurants), t(:Other_user_published_restaurants),
	    restaurantes_user_path(@user)) %>
  </li>
</ul>

<% title "#{t(:favorite_restaurants_of)} #{@user.login}" %>
<h2 class="title"><%= yield(:title) %></h2>

<%= render :partial => 'user_restaurants' %>
