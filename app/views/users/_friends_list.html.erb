<h1 class="title"><%= title %></h1>
<table>
  <% for user in users %>
    <tr>
	  <th></th>
	  <th>Nick</th>
	  <th>Puntuación</th>
	  <th>Recetas publicadas</th>
	  <th></th>
	</tr>
	<tr>
	  <td><%= image_tag(user.avatar.url(:small)) %></td>
  	  <td><%= link_to user.login, user_path(user) %></td>
	  <td><%= user.puntuation %>/5</td>
	  <td><%= user.recetas.count %></td>	
	  <td><span id="my_friend_<%=user.id%>"></span><td>
	  <td><%= link_to_remote 'Invitar amigo',
	    :loading => "$('spiner_#{user.id}').show();",
	    :complete => "$('spiner_#{user.id}').hide();", 
	    :url => {:controller => 'friendships', :action => 'create', 
		:users => {:user_id => current_user.id, :friend_id => user.id}},
		:html => {:id => "my_link_#{user.id}"}%></td>
	  <td><%= image_tag('web/spinner.gif', :id => "spiner_#{user.id}",
        :style=> 'display:none;')%></td>
	</tr>
  <% end %>
</table>
