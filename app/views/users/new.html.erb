<% title t(:register) %>
<h2 class="title"><%= yield(:title) %></h2>

<% if @user.errors.blank? %>
	<div class="signup">
	  <div class="message-ok">
	    <%= t(:register_intro)%>
	  </div>
	</div>
<% end %>

<%= render :partial => "form"%>
