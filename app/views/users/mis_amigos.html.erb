<%= friends_link('Mis amigos', 'mis_amigos', current_user.friends.size)%>
<%= friends_link('Invitaciones recibidas', 'invitaciones_recibidas', current_user.friends.size)%>
<%= friends_link('Mandar invitaciones', 'invitar', current_user.friends.size)%>
<%= friends_link('Invitaciones enviadas', 'invitados', 1)%>

<%= render :partial => 'friends_list', :locals => {:users => @users, :title => @title}%>

