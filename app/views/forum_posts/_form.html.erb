<% form_for [@forum_cat_l2,@forum_post] do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :Título %><br />
    <%= f.text_field :title %>
  </p>

	<p>
		<%= f.label :Post %><br />
	    <%= f.text_area :comment %>	
	</p>

  <p>
    <% if action_name == "new" %>
		<%= f.submit "Postear" %>
	<% else %>
		<%= f.submit "Editar" %>	
	<% end %>
	| <%= link_to 'Volver', forum_posts_path %>
	
  </p>
<% end %>